<mjml>
  <mj-head>
    <mj-title>{{title}}</mj-title>
    <mj-preview>{{preheader}}</mj-preview>
    <mj-attributes>
      <mj-all font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif" />
      <mj-button font-weight="600" border-radius="8px" padding="16px 32px" />
      <mj-text line-height="1.6" />
    </mj-attributes>
    <mj-style inline="inline">
      .primary-color { color: #e63946 !important; }
      .primary-bg { background-color: #e63946 !important; }
      .gradient-bg { background: linear-gradient(135deg, #e63946 0%, #ff6b6b 100%) !important; }
      .security-icon { font-size: 32px; margin-bottom: 16px; }
      .code-box {
        background: #f7fafc !important;
        border: 2px solid #e2e8f0 !important;
        border-left: 4px solid #e63946 !important;
        padding: 16px !important;
        border-radius: 8px !important;
        margin: 20px 0 !important;
        word-break: break-all !important;
      }
      .security-tip {
        background: #e6fffa !important;
        border-left: 4px solid #38b2ac !important;
        padding: 16px !important;
        border-radius: 6px !important;
        margin: 20px 0 !important;
      }
      @media only screen and (max-width: 600px) {
        .mobile-padding { padding: 20px !important; }
      }
    </mj-style>
  </mj-head>
  <mj-body background-color="#f8fafc">
    <!-- Main Container -->
    <mj-wrapper background-color="#ffffff" border-radius="12px" padding="0" css-class="shadow">

      <!-- Header with Gradient Background -->
      <mj-section css-class="gradient-bg" padding="40px 20px">
        <mj-column>
          <!-- Logo/Icon -->
          <mj-text align="center" css-class="security-icon">
            🔐
          </mj-text>
          <!-- App Name -->
          <mj-text align="center" color="#ffffff" font-size="32px" font-weight="bold" padding="0">
            {{appName}}
          </mj-text>
          <!-- Subtitle -->
          <mj-text align="center" color="rgba(255,255,255,0.9)" font-size="16px" font-weight="400" padding="8px 0 0 0">
            安全 • 便捷 • 统一的身份认证平台
          </mj-text>
        </mj-column>
      </mj-section>

      <!-- Main Content -->
      <mj-section padding="40px 30px" css-class="mobile-padding">
        <mj-column>

          <!-- Greeting -->
          <mj-text font-size="20px" color="#2d3748" font-weight="600" padding="0 0 20px 0">
            您好！
          </mj-text>

          <!-- Main Message -->
          <mj-text font-size="16px" color="#4a5568" padding="0 0 30px 0">
            感谢您注册 <strong>{{appName}}</strong>！为了确保您的账户安全，请点击下方按钮验证您的邮箱地址。
          </mj-text>

          <!-- CTA Button -->
          <mj-button
            css-class="primary-bg"
            color="#ffffff"
            font-size="16px"
            href="{{verificationUrl}}"
            target="_blank"
            rel="noopener noreferrer"
          >
            验证邮箱地址
          </mj-button>

          <!-- Alternative Link Section -->
          <mj-text font-size="14px" color="#4a5568" font-weight="600" padding="30px 0 10px 0">
            无法点击按钮？
          </mj-text>
          <mj-text font-size="14px" color="#718096" padding="0 0 10px 0">
            请复制以下链接到浏览器地址栏：
          </mj-text>
          <mj-text
            font-size="12px"
            color="#4a5568"
            font-family="Monaco, Consolas, 'Lucida Console', monospace"
            padding="0 0 20px 0"
            background-color="#f7fafc"
            css-class="code-box"
          >
            {{verificationUrl}}
          </mj-text>

          <!-- Important Notice -->
          <mj-text font-size="16px" color="#4a5568" padding="30px 0 0 0">
            <strong style="color: #e53e3e;">⚠️ 重要提醒：</strong>
          </mj-text>

          <mj-text font-size="15px" color="#4a5568" padding="10px 0 0 0">
            • 此验证链接将在 <strong>24 小时</strong>后过期<br/>
            • 如果您没有注册 {{appName}} 账户，请忽略此邮件<br/>
            • 请勿将此链接分享给他人，以保护您的账户安全
          </mj-text>

          <!-- Security Tips -->
          <mj-text font-size="14px" color="#234e52" font-weight="600" padding="30px 0 8px 0" background-color="#e6fffa">
            🛡️ 安全提示
          </mj-text>
          <mj-text font-size="14px" color="#234e52" padding="8px 0 20px 0" background-color="#e6fffa">
            {{appName}} 永远不会通过邮件要求您提供密码、验证码或其他敏感信息。
          </mj-text>

        </mj-column>
      </mj-section>

      <!-- Footer -->
      <mj-section background-color="#f7fafc" padding="30px" border-top="1px solid #e2e8f0">
        <mj-column>

          <!-- Help Text -->
          <mj-text align="center" font-size="14px" color="#718096" padding="0 0 15px 0">
            需要帮助？联系我们的客服团队
          </mj-text>

          <!-- Footer Links -->
          <mj-text align="center" font-size="14px" padding="0 0 20px 0">
            <a href="{{baseUrl}}/docs" style="color: #e63946; text-decoration: none; margin: 0 12px;">帮助中心</a>
            <a href="{{baseUrl}}/privacy" style="color: #e63946; text-decoration: none; margin: 0 12px;">隐私政策</a>
            <a href="{{baseUrl}}/terms" style="color: #e63946; text-decoration: none; margin: 0 12px;">服务条款</a>
          </mj-text>

          <!-- Copyright -->
          <mj-text align="center" font-size="12px" color="#a0aec0" padding="0">
            © {{currentYear}} {{appName}}. 保留所有权利。
          </mj-text>

          <!-- Unsubscribe -->
          <mj-text align="center" font-size="11px" color="#cbd5e0" padding="10px 0 0 0">
            这是一封系统自动发送的验证邮件，请勿直接回复。
          </mj-text>

        </mj-column>
      </mj-section>

    </mj-wrapper>

    <!-- Bottom Spacer -->
    <mj-section padding="20px">
      <mj-column>
        <mj-spacer height="1px" />
      </mj-column>
    </mj-section>

  </mj-body>
</mjml>
